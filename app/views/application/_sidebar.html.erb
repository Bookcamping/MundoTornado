<h4>¡Estás en <%= current_group.name %>!</h4>
<p><%= markdown current_group.description %></p>
<ul>
  <% current_group.pages.each do |page| %>
    <li><%= link_to page.title, page %></li>
  <% end %>
</ul>
